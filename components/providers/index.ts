/**
 * Provider Components
 *
 * Export all provider components for convenient imports:
 * import { SmoothScrollProvider, MotionProvider } from '@/components/providers';
 */

export {
  SmoothScrollProvider,
  useSmoothScroll,
  ScrollTo,
} from './SmoothScroll';

export { MotionProvider } from './MotionProvider';
